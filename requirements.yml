#? ansible-galaxy install -r requirements.yml --force
#? https://docs.ansible.com/ansible/latest/galaxy/user_guide.html#install-multiple-collections-with-a-requirements-file

---
roles:
  # # Install a role from Ansible Galaxy.
  # - name: geerlingguy.java
  #   version: "1.9.6" # note that ranges are not supported for roles
# from GitHub
  # - src: https://github.com/DanielWeeber/ansible-role-windows_exporter
  # - src: https://github.com/fabricesemti80/ansible-role-windows_exporter

  - name: geerlingguy.docker

collections:
  # Install a collection from Ansible Galaxy.

  - name: community.general
    type: galaxy
